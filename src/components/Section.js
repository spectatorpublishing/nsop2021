import styled from 'styled-components';
import Header from './SectionHeader';

const Wrapper = styled.div`

`;

const Section = ({sectionName}) => {
    return (
        <>
            <Header title="Student Life" />
            <Wrapper>

            </Wrapper>
        </>
    )
};

export default Section;